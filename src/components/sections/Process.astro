---
// Process — 四步执行流程横排（浅色渐变底）
// 圆形图标内放 mono 步骤号；步与步之间用可伸缩虚线连接；滚动进入按 90ms 级差依次点亮（线性叙事）
import { process } from '../../data/home';
---

<section class="process" id={process.id} data-section={process.id}>
  <div class="shell">
    <div class="section-heading reveal">
      <p class="eyebrow">{process.eyebrow}</p>
      <h2>{process.heading}</h2>
    </div>
    <div class="process-track">
      {
        process.steps.map((step, index) => (
          <>
            {index > 0 && <span class="process-line reveal" style={`--reveal-delay: ${index * 90}ms`} aria-hidden="true" />}
            <article class="process-step reveal" style={`--reveal-delay: ${index * 90}ms; --reveal-distance: 14px`}>
              <span class="step-icon">{step.number}</span>
              <h3>{step.title}</h3>
              <p>{step.description}</p>
            </article>
          </>
        ))
      }
    </div>
    <div class="process-cta reveal" style="--reveal-delay: 360ms">
      <a class="button button-outline-dark" href="/contact">Talk to Sales</a>
    </div>
  </div>
</section>

<style>
  .process {
    padding: var(--space-9) 0;
    /* 渐变终点直接接下一区底色（surface-tint = Network 区同源），消除"两种近白"接缝 */
    background: linear-gradient(180deg, var(--color-surface), var(--color-surface-tint));
  }

  .section-heading {
    text-align: center;
    margin-bottom: var(--space-7);
  }

  .section-heading h2 {
    font-size: var(--text-h2);
    line-height: 1.12;
    letter-spacing: var(--tracking-tight);
    margin: 0;
  }

  .process-track {
    max-width: 1160px;
    margin-inline: auto;
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
  }

  .process-step {
    position: relative;
    z-index: 1; /* 白底圆遮住延伸进来的连接线 */
    flex: 0 1 236px;
    text-align: center;
  }

  /* 圆形图标：mono 步骤号；随 reveal 依次"点亮"（90ms 级差 = 流程叙事） */
  .step-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 74px;
    height: 74px;
    border-radius: 50%;
    border: 1px solid rgba(var(--color-accent-rgb), 0.22);
    background: var(--color-surface);
    color: var(--color-accent);
    font-family: var(--font-mono);
    font-size: 22px;
    font-weight: 700;
    box-shadow: 0 1px 2px rgba(10, 29, 51, 0.05);
    transition:
      border-color var(--duration-base) var(--ease-out),
      background-color var(--duration-base) var(--ease-out),
      box-shadow var(--duration-base) var(--ease-out);
  }

  .process-step.reveal.visible .step-icon {
    border-color: var(--color-accent);
    background: var(--color-surface-tint);
    box-shadow: 0 0 0 6px rgba(var(--color-accent-rgb), 0.06);
    transition-delay: var(--reveal-delay, 0s);
  }

  .process-step h3 {
    font-size: 17px;
    letter-spacing: var(--tracking-tight);
    margin: var(--space-5) 0 var(--space-2);
  }

  .process-step p {
    font-size: 14px;
    line-height: 1.65;
    color: var(--color-text-muted-on-light);
    margin: 0;
  }

  /* 步间连接线：从圆边延伸到圆边（负 margin 吃掉步内文字死区），z-index 让白圆压线 */
  .process-line {
    flex: 1;
    height: 1px;
    margin-top: 37px;
    margin-inline: -73px;
    background: repeating-linear-gradient(90deg, var(--color-accent) 0 6px, transparent 6px 12px);
    opacity: 0.55;
  }

  .process-cta {
    text-align: center;
    margin-top: var(--space-7);
  }

  @media (max-width: 760px) {
    .process {
      padding: var(--space-8) 0;
    }
    /* 横向滚动，保持流程的线性叙事 */
    .process-track {
      justify-content: flex-start;
      gap: var(--space-4);
      overflow-x: auto;
      padding-bottom: var(--space-4);
      -webkit-overflow-scrolling: touch;
    }
    .process-step {
      flex: 0 0 168px;
    }
    .process-line {
      flex: 0 0 30px;
      margin-inline: 0;
    }
  }
</style>
